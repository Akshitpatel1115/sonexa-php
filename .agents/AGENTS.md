# Git Commits
Whenever you finish making changes or completing a task, ALWAYS output the appropriate `git commit` and `git push` commands with a proper, descriptive commit message at the very end of your response.
